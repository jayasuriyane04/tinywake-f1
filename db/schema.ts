import { sqliteTable, text, integer, real, index } from 'drizzle-orm/sqlite-core';
export const devices = sqliteTable('devices', {id:text('id').primaryKey(),name:text('name').notNull(),online:integer('online').notNull(),rssi:integer('rssi'),config:text('config').notNull(),version:integer('version').notNull().default(1),createdAt:integer('created_at').notNull()});
export const sessions = sqliteTable('sessions', {id:text('id').primaryKey(),deviceId:text('device_id').notNull().references(()=>devices.id),text:text('text').notNull(),source:text('source').notNull(),latency:real('latency'),confidence:real('confidence'),duration:real('duration').notNull(),bytes:integer('bytes').notNull(),createdAt:integer('created_at').notNull(),scenario:text('scenario').notNull()},t=>[index('sessions_created_at').on(t.createdAt),index('sessions_device_created').on(t.deviceId,t.createdAt)]);
export const settings = sqliteTable('settings',{key:text('key').primaryKey(),value:text('value').notNull()});
export const events = sqliteTable('events',{id:text('id').primaryKey(),type:text('type').notNull(),deviceId:text('device_id'),detail:text('detail').notNull(),createdAt:integer('created_at').notNull()},t=>[index('events_created_at').on(t.createdAt)]);
